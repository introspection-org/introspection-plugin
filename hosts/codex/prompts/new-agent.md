Build a custom managed Introspection agent from the user's description.

Read `~/.codex/introspection/core/skills/agent-builder-flow/SKILL.md` and
follow it end to end (preflight → interview → scaffold → validate →
review/commit → publish/wire-up → first run → wrap-up), together with the
`recipe-authoring` and `platform-onboarding` skills next to it. Run the
interview and scaffold steps yourself — there is no separate builder agent
in this host.

Nothing is committed, pushed, published, or registered without the user
approving what they've seen, and no credential value ever goes into a recipe
file.

Agent description: {{args}}
