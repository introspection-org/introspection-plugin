interface:
  display_name: "Harbor"
  short_description: "Implement approved offline environment evals"
  default_prompt: "Use $introspection:harbor to implement and validate this human-approved offline Harbor eval, then hand durable coverage to Recipes."
