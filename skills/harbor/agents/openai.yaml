interface:
  display_name: "Harbor"
  short_description: "Create trustworthy environment-level evals"
  default_prompt: "Use $introspection:harbor to create and validate this Harbor evaluation task with the official Harbor skills."
