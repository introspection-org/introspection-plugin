interface:
  display_name: "Create"
  short_description: "Build and locally prove a new agent"
  default_prompt: "Use $introspection:create to define, build, and locally prove a new agent recipe with Pi."
