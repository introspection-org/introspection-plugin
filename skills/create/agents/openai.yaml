interface:
  display_name: "Create"
  short_description: "Create, migrate, and locally prove agents"
  default_prompt: "Use $introspection:create to build or migrate an agent into a locally proven recipe."
