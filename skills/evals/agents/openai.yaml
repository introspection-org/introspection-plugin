interface:
  display_name: "Evals"
  short_description: "Turn agent traces into trustworthy measures"
  default_prompt: "Use $introspection:evals to analyze these agent traces, discover failure modes, and build trustworthy measurements."
