interface:
  display_name: "Evals"
  short_description: "Turn agent traces into trustworthy measures"
  default_prompt: "Use $evals to analyze these agent traces, discover failure modes, and build trustworthy measurements."
