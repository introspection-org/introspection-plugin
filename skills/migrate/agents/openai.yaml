interface:
  display_name: "Migrate"
  short_description: "Convert an existing agent into an agent recipe"
  default_prompt: "Convert this existing agent into an agent recipe, preserve its approved behavior, and test it locally with representative cases."
