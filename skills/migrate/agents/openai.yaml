interface:
  display_name: "Migrate"
  short_description: "Convert an existing agent into a recipe"
  default_prompt: "Convert this existing agent into a locally proven Introspection recipe, preserving its approved behavior."
