interface:
  display_name: "Deploy"
  short_description: "Deploy and verify a tested agent recipe"
  default_prompt: "Deploy this agent recipe after testing it locally with representative cases, then verify its Introspection runtime in staging."
